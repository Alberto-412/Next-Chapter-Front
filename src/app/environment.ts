export const environment = {
  apiUrl: 'http://localhost:10200/api'    //Guardamos la URL base del backend en un sitio centralizado. Así si algún día cambia la URL solo hay que cambiarla aquí y afecta a todo, en lugar de tener que buscarla por todos los archivos.
};